#include <stdio.h>

void insert(char *name, int flag) {
    printf("insert🔥 : %s\n", name);
}
void lookup(char *name) {
    printf("lookup✨ : %s\n", name);
}
void delete(char *name) {
    printf("delete😭 ; %s\n", name);
}